server {
    listen 80;
    server_name 112.126.27.148;

    location /fund-dashboard {
        alias /var/www/frontend/fund-dashboard/dist/;
        index index.html;
        # 先不要写 try_files，看看能不能出页面
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000/;
        proxy_set_header Host $host;
    }
}
